// Paste your Mapbox public token below. This file is committed with a placeholder.
// Domain-restricted tokens are safe in frontend code but GitHub push protection blocks them.
// To configure: replace REPLACE_ME with your token locally, do not commit the real value.
window.MAPBOX_TOKEN = 'REPLACE_ME';
